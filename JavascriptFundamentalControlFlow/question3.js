/**
 * 3. Sebuah toko fashion sedang melakukan discount besar-besaran untuk setiap produk yang mereka jual.
 * Rules discount sebagai berikut:
 * - kemeja -> 20%
 * - celana -> 45%
 * - kaos -> 35%
 * NOTE: Untuk mendapatkan discount, harus membeli masing-masing product diatas Rp. 100,000.
 *
 * Pembeli bernama Angel membeli barang dengan kriteria sebagai berikut =>
 * - 2 Kemeja dengan harga masing-masing Rp. 40,000 dan Rp. 120,000
 * - 1 Celana dengan harga Rp. 85,000
 * - 4 Kaos dengan harga masing masing Rp. 25,000
 *
 * Berapa harga final yang harus dibayar Angel untuk seluruh produknya?
 * NOTE: Rumus menghitung harga barang discount -> (harga barang * discount%).
 */

const namePembeli = 'Angel';
const hargaKemeja1 = 40000;
const hargaKemeja2 = 120000;
//* Lanjutkan code untuk inisialisasi barang yang dibeli Angel

const minimumPembelian = 100000;
const discountKemeja = 20;
//* Lanjutkan code untuk inisialisasi discount

let hargaFinalKemeja = 0;
let hargaFinalCelana = 0;
//* Lanjutkan code untuk inisialisasi variable harga akhir setiap barang (gunakan Let)
// let hargaFinalSeluruhBarang = hargaFinalKemeja + hargaFinalCelana + ...

//* Lanjutkan code untuk menghitung harga dari masing-masing barang setelah mendapat discount

//* Lanjutkan code untuk menghitung total harga barang yang dibeli Angel

//* Tampilkan output nama dan harga total seluruh barang yang dibeli Angel
console.log('');
